# 改动记录

> 版本号：改一次末位 +1，末位满 10 进位。

## 1.0.0

> 这一版动的是**切词**和**打分**两块底子，所以进了一位。

### 换了切词器：改用通用中文词表

原来判断「这句话跟这条记忆有没有关系」，用的是**字符串包含**：
把问句切出来的词，拿去记忆正文里搜有没有这个字串。

之所以只能用这种硬撞的办法，是因为老切法**会丢字**：它拿停用词当刀把句子切开，
切完的碎片对不上原文（「她的胃不好」被切成「胃不好」，「她的」凭空没了），
问句「她的胃」就只剩单字「胃」，跟记忆那边的「胃不」对不上。
于是只能退回字符串包含，把切词器的信息损失糊过去。

代价是**一个字会撞进长词里**：

- 问句里单独出现一个「车」→ 撞上记忆里的「**车**站」
- 「心」→ 撞上「关**心**」
- 「书」→ 撞上「**书**包」

这些字在库里都很稀有，于是被当成**强证据**，把不相干的记忆顶到最前面。

现在改用词表切：词表认得「车站」是一个词，那一段就**整段吃掉**，
「车」不再单独冒出来；而「那辆车」里的「车」是独立词，照样留着。
两边**终于能按词比**了。

- 词表用 jieba 的 `extra_dict/dict.txt.small`（MIT 协议，见 `lib/词典-来源.txt`），
  只保留词条本身，去掉了词频和词性两列
- 词表是**通用中文**的，跟谁用、说过什么话都无关 —— **装上第一天就管用**，
  不需要先喂够语料
- 最长匹配 + 单字兜底，**一个字都不丢**；兜底的单字如果是虚词（的／了／我）就不当线索
- 读不到词表文件时**自动退回老切法**，不会崩
- 有词表时**关掉「半个词」那道过滤**。那套（碎／整／邻熵）是给老切法擦屁股的 ——
  老切法会造出「天去」「去公」这种跨词碎片，才需要挑出来扔掉；
  词表切出来的是真词，再过滤只会**误杀真词**

### 打分：只拿「稀有词」算证据

治的是「**五个弱证据攒起来，看着像强证据**」。

原来命中的词是**按稀有度直接相加**的，于是「晚上＋会＋完＋吃＋面」这种
五个都不相关的词凑起来能把一条不相干的记忆顶到 0.73，
而真正相关的那条只撞上两个词、反而排不上去。

现在：**一个词如果在库里 1% 以上的记忆里都出现，它就不是线索，不拿它算分。**

- 「的」「了」「会」「完」自然被压掉；少见的具体词留下
- **不用维护虚词表** —— 中文虚词几百个，列不完，列了也是拍脑袋，
  而且列出来的表对别人不一定对
- 库里小的时候（刚装上）阈值自动变小 → 几乎不压 → 不会误伤
- 全被压光时**退回不压**（安全垫），免得一句话一个词都不剩

### 不硬凑条数（0.9.9 那批修补里漏掉的一处）

条数上限**跟着最高分走**：最高分很高（确实有一句很贴）→ 该给的都给；
最高分很低（一句贴的都没有）→ 只给 1 条，别硬凑。

- 位置很要紧：这个上限**在去重之后**才用，不然重复的会把名额吃掉
- 早先试过「相对门槛」（`C >= 0.7 × 最高分` 就留），**误杀**：
  分数低不等于不相关，只是那条撞得没那么准 → 放弃了

### 量出来的效果

拿 40 句真实问句、782 条真实记忆量。判据是「跟问句共享一个
**库里最多 3 条有的词**」（跟打分用的门槛不同，避免自己量自己）：

| | 前 3 名里捞到相关的 | 前 3 名精确度 |
| --- | --- | --- |
| 改之前 | 63% | 43% |
| 改之后 | 88% | 91% |

- 一轮召回 **2.3 ms**（比原来还快：按词比省掉了整段文本的字符串搜索）
- 词表 898 KB，**只在本地当查找表用，跟每轮对话发出去的 token 无关**

## 0.9.9

### 新功能：从会话生成档案

把会话读一遍，让模型提炼出一份长期档案（固定九章），再走已有的
「从旧档案导入」变成记忆条目。四步分开，因为**第三步要调模型、要花钱**，
得能一批一批停下来看：

- 记忆列表那一行加了个「从会话生成档案」按钮，点开是四步面板
- **① 选会话** —— 默认一个都不勾（不替用户决定要处理哪些）；
  有「全选 / 全不选」
- **② 算一下要多少** —— 这一步**不调模型、不花钱**，只报批数、字数、粗算 token
- **③ 开始跑** —— 一批一批调模型；跑完一批就把条目攒起来，
  **下一批会带上「已经记过的」清单**，同一件事不会被收两遍
- **④ 存到哪儿** —— 能直接改，也能点「选文件夹」挑；目录不存在会自动建；
  给的是目录（结尾带分隔符、或本身是已存在的目录）会自动补上文件名；存之前备份

会话列表这块：

- **显示会话名字**（原来只有 `session-xxxx`，没法挑）
- 按 **DSH 自己的名单**列（不再去翻磁盘目录 —— 磁盘上会有 DSH 早就不显示的残留）
- 过滤掉：**归档的**、**子代理跑出来的**、**一句话都没说过的空壳**
- 子代理靠会话头里的 `delegationDepth > 0` 认出来（只解第一帧，便宜）

### 召回质量

- **稀有度夹到 1 以内。** 原来没夹：一个在库里**从来不是一个词**的字符串
  （`df = 0`）会算到 **1 以上** —— 实测某条拿到 1.12，比任何真词都高。
  而 `df = 0` 多半意味着那是假词或单字，**越假反而越值钱** ✗
- **打分乘上「说话稀有度」。** 这张尺子（从用户自己的话里长出来，
  天天挂在嘴边的词不算信息）原来只用在**问句门槛**上，没用在**打分**上 ——
  于是「宝宝」这种天天喊的词照样当满额证据。现在它也参与打分。
  样本少于 20 句时不压，免得新会话被误伤。
- **召回去重门槛另开一个常量（0.80），不再跟「查重合并」共用 0.85。**
  合并是**真删东西、不可逆**，必须保守；召回时去重只是**这一轮不显示它**，
  东西还在库里、下一轮照样能想起来 —— 风险小得多，就该用更宽的门槛。

### 修

- 「选文件夹」选过一次之后再想换会卡住：再打开时**不重新读目录**，
  看到的还是上次那份。现在每次打开都重读，并且**从当前路径所在的目录开始**
  （不用每次从盘符点起）；选择器里也放了一个「收起」
- 自检套件加了 180 秒超时。有的自检结尾是 `if (挂 > 0) process.exit(1)` ——
  **挂 = 0 时不退出**，而它里面有挂着的定时器 → 进程永远不结束 →
  整套自检看起来像卡死，最后那行总结永远打不出来 ✗

## 0.9.8

- 「补标不淡 / 重判一遍」的进度条**挪到它自己的按钮下面**。
  原来它跟「扩词」「提炼」挤在同一张卡片里 —— 点了重判要往上翻才看得见，
  没点的人还会以为那条是自己点出来的 ✗
  现在谁点的进度条就跟在谁的按钮旁边，**点了才出现，没点就没有**；
  条上写明是「补标不淡」还是「重判一遍」。
- 「补标不淡」的判据补了一条例外：**日期只是一票，不是否决票**。
  原来「以日期开头一律判成会淡」是一票否决，于是「某天定下、往后一直算数」
  那类被整条判淡了 —— 比如纪念日、定下来的口令、说好一辈子的事。
  现在这类**以日期开头也判不淡**，判据看的是：把日期拿掉之后，
  剩下的话是不是一条往后一直成立的说法，而不是一段「那天怎么了」的经过。
  纯流水账仍然判会淡，没有放松。

## 0.9.7

- 「已删掉的」那一栏的工具条**挪到最上面**。
  原来它在列表最下面 —— 几十条的时候得先翻到底才能点「全选」。
  现在紧挨着标题：全选 / 批量清掉 / 取消勾选 / 全部清空。

## 0.9.6

- 修「补标不淡」**跑到一半自己停掉**：判断「跑完没」原来用的是「这批判回了几条」——
  某一批模型没回东西（判回 0 条）就被当成「判完了」，直接停下 ✗
  （实际记录里是「判了 0 条，还剩 665」，然后就没了）
  - 改成看服务端返回的 `done`；一批回 0 条就**接着试下一批**
  - 连着 3 批都空才停，并说清楚「再点一次会接着跑，不会重头来」

## 0.9.5

- **「补标不淡」配了进度条**。它原来只有一行字，跑起来看不出在动 ——
  其实一直在跑（一批批都有记录），但界面上像卡住了。
  现在有一条进度条，写着「补标不淡：已判 N / 总共 M 条（X%）　还剩 K 条」，
  撞限流等待时会写明「正在等 N 秒」。

## 0.9.4

- 「已删掉的」那一栏补了**全选 / 取消全选**：
  点「全选（N）」一下全勾上（按钮变成「取消全选」），再点一下取消。
  勾完照样走「批量清掉」的两次确认。

## 0.9.3

- **「已删掉的」那一栏也能批量清掉了**。原来只有「一条条清」和「全部清空」，
  想清掉其中几条得一条条点。现在：
  - 每行一个勾选框 → 勾上几条就出现「**批量清掉（N）**」
  - **点第一次不清** —— 只出确认条；在确认条上**再点一次**才真清（这个是销毁，所以要问两遍）
  - 跟「批量删」用的是各自独立的勾选，两栏不会互相带

## 0.9.2

- 判据补一条：**没有日期的，按内容本身判**，别按长短 ——
  讲性格、关系、称呼、喜好、身体和病史、长期状况 → 1；
  讲某一次对话、某一个瞬间、某一时的心情、某一次的数据 → 0.5。
  0.9.1 只写了「以日期开头的默认 0.5」，没有日期的那些缺一条判据，
  结果把一批讲性格、讲关系定义的也判成了会淡的。

## 0.9.1

- **「补标不淡」的判据写严了**。原来判得太松：把一批「某天发生了什么」的
  流水账也判成了「要一直记得」（实测判过的里面 82% 都判成了长期事实）。
  新判据：
  - **以日期开头的，默认一律 0.5** —— 哪怕那件事很重要、很感人、是第一次、是求婚、是婚礼。
    「重要」不等于「一直成立」：重要的事值得记住，但它记的是「那天」。
  - **唯一的例外**：整条就是「约定 / 规则 / 承诺的条文」本身。而且要同时满足三条：
    删掉日期后仍是一条完整可照做的规定；是「往后一直照做」的；不夹杂当天的时间、
    金额、动作、心情、地点等流水细节。缺一条就回 0.5。
  - 兜底收紧：日期开头的条目，**只有能原样引用出一句条文**时才给 1。
- 修：列表接口没把「判过没」发给页面 —— 于是「补标不淡」的计数永远是全库条数，
  看着像要从头重来（服务端本来就只判没判过的）。
- 新增**「重判一遍」**：判据改过之后，把老结果清掉用新判据重来一遍。
- 「补标不淡」撞上限流（429）时**自动等一等再试**（5s→10s→20s→40s→60s，最多 6 次），
  不再直接失败；每批之间的间隔也从 0.3 秒放慢到 1.5 秒。

## 0.9.0

- **「不会淡」的判据改回「长期事实」**（`category === 'stable_profile'`）。
  0.8.9 曾用「鲜活度是否恒定」当判据，那是打分实现细节，不是语义，
  会把一批没有日期的普通事件误收进「不会淡」里。
- **没有日期的记忆现在也会淡**：以「记录时间」（`createdAt`）为衰减锚点。
  此前这类记忆的鲜活度被钉在常数上，看起来永远不淡。

## 0.8.9

- **「不会淡的」单独收进一个折叠分组**，外面只列会淡的。
  长期事实是要一直记得的事，不该和普通事件混在一列里。
- 长期事实的鲜活度一栏显示「不淡」，不再显示百分比。

## 0.8.8

- 新增**批量删除**（两次确认）：
  每行一个勾选框 → 出现「批量删（N）」→ 点一下只出确认条 → 再点「确定删」才真删。
  删掉的收进「已删除」，可以找回；操作前自动备份。

## 0.8.7

- 修「鲜活度」显示：
  - 列表页原本自己算了一套公式，和召回实际使用的不是同一套；
    没有日期的记忆会算出 `NaN`，页面显示成「鲜活 NaN%」。
    现在两边共用同一个函数。
  - 长期事实不再显示「100%」（它本来就不淡，显示百分比会误导）。
- 日期显示两处：
  - **未来的日期被显示成「今天」**（`days <= 0` 把负数也算进去了）。
  - 47 天被四舍五入说成「2 个月前」，改成向下取整。

## 0.8.6

- **正文里提到的「以后」的日期不算事件日期**。
  例如一条记录规则的消息里写了「下次 X 月 X 日」，
  原来会把那个未来日期当成这条记忆的发生时间。

## 0.8.5

- **重写日期提取**：
  - 开头的日期才是事件日期，认到就不再往后找。
  - 句中提到的生日 / 出生 / 纪念日一律不算。
  - 只有生日、没有事件日期时留空。
  - 「vs / 还是 / 不是」这种比较或纠正数字的语境不算日期。
  - 年份改为「取离现在最近的那一年」（原来一律按过去算，
    会把明天的日期算成去年）。

## 0.8.4

- 修「查重」两个问题：
  1. 归组原用并查集（传递闭包），A 像 B、B 像 C 就归为一组，
     但 A 和 C 可能并不相似，一组会串成杂烩。
     改成按种子归组：每条只和种子比较。
  2. 合并接口原本不校验，客户端传什么就删什么。
     改成服务端自己再算一遍相似度，不够像的一条都不收，并列出来。
- 合并请求改为传「组」（保留哪条 + 删除哪些）。

## 0.8.3

- 新增**查重**入口（列表标题行）。去重此前只在召回和导入时自动进行，
  库里已有的重复没有入口。
  - 分两类：**真重复**（两条长度接近，可一键合并）和
    **汇总 + 明细**（一长一短，只列出让你自己挑）。
  - 默认保留最长的一条；每组可单独选择保留哪条。
  - 合并是软删除，收进「已删除」，可以找回。
- 修：会话详情页看不到任何结果提示。
  那些提示原来只在第一级渲染，而详情页是提前返回的。

## 0.8.2

- **涉及第三方时必须写名字**（助手、别的 AI、孩子、家人、朋友）。
  提炼此前只有「我 / 她」两个人称，第三方会被并进去，
  导致「某人做的事」被记成「我做的」或「她做的」。

## 0.8.1

- 提炼的人称规则补三条：
  - 原文已有主语的，一个字都不许换。
  - 宁可分成几句写，也不要为了通顺把几个分句揉成一句（一揉主语就串）。
  - 只有说得出依据才补主语，说不出依据就留空。

## 0.8.0

- **「补标不淡」改成能上也能下**。
  原来只找「该标记却没标记」的，治不了另一半：
  「不该标记却标记了」。现在所有没判过的一起判，
  模型说 1 就升成长期事实，说 0.5 就降回普通事件。

## 0.7.9

- 设置页整理：「参数」默认收起；「备份」说明移到末尾。
- 图标 🐟 → 🐋。

## 0.7.8

- **导入时也做近似去重**：相似度足够高时，新的更全就补全旧的，否则跳过。
  阈值比召回去重（0.85）高，取 0.9 —— 导入不可逆，宁可漏几条重复。

## 0.7.7

- 「长期事实」单独收成一栏（折叠分组）。
- 新增**补标不淡**按钮：拿模型一条条判「该不该一直记得」。

## 0.7.6

- 提炼接上「永远不淡」：顺手判这一条该不该一直记得。
- 「重要的日子」（生日、纪念日、结婚、第一次）补进判据清单。
- 寒暄、撒娇、甜话的汇总在提炼时判「跳过」。

## 0.7.5

- **召回去重**：注入前把几乎一样的只留一条（留分高的）。

## 0.7.4

- `package.json` 补 `keywords` / `repository`，整理 README 与格式。

## 0.7.3

- 修开关：关掉之后注入会立刻停。

## 0.7.2

- 修安装说明：包没有发布到 npm，改用 GitHub 形式安装。
- 提炼不再写死用户称呼（跟随设置）；出生日期不当成事件时间；
  短记忆门槛从 8 字降到 6 字。

## 0.7.1

- 导入健壮性一批（13 处）：一整段没分行、单行超长、子文件夹、
  各种项目符号与括号编号、零宽空格、时区不写死、不存在的日期等。

## 0.7.0

- 发布到 GitHub（MIT）。此前只在本机使用。

## 0.6.x

- 0.6.6 抠日期强化 + 「补日期」按钮。
- 0.6.5 跨年显示必须带年份。
- 0.6.4 没有时间戳、但被再次提到的记忆要能涨、也要会淡。
- 0.6.3 **门槛从卡 P 改成卡 C（证据）**，鲜活度只参与排序。
- 0.6.2 正文不撑破容器（`minWidth: 0` + `overflowWrap`）。
- 0.6.1 记忆列表按日期排序。
- 0.6.0 幽灵会话不再显示；归档筛选不再静默失败；版式整理。

## 0.5.x 及更早

- 记忆段从 `systemPrompt.section()` 改成 `context()`：
  每轮都变的内容放进系统提示词会让整段缓存失效。
- trace 瘦身 + 切词缓存（每轮开销从约 57 ms 降到约 8 ms）。
- 提炼（档案原文 → 第一人称记忆）、扩词、导入、召回面板、
  记忆列表检索与折叠、复制记忆到别的会话、归档会话不再显示。
