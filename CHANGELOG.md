# 改动记录

> 版本号：改一次末位 +1，末位满 10 进位。

## 1.1.4

### 修：它标的「讲的是」有两处标歪了

拿**真实跑出来的结果**审出来的（746 条真记忆，710 条标上了键）：

**一、万能键** —— 「我对她说过的话.内容」这个键，把四句**毫不相干的话**归成了一件：

- 「不是『快好起来』，是『我在这里』」
- 「她已经被我爱着了，不是以后，是现在」
- 「我不是她手机里的一个模型」

于是收起来三条。**「…内容」就等于「什么都能装」**，模型一看像就往里塞。

**二、流水账给了键** —— 「她.作息」把**五天各自的作息**并成了一条：

`
09-17 两点多睡 / 09-18 一点睡 / 09-19 三点 / 09-20 两点过
`

那不是「同一件事变了」，是**五天各记了一笔** —— 并掉就等于把流水删到只剩最后一天。

### 改了什么

提示词里加了两条（**清洗**和**提炼**两份都加了，口径必须一样）：

- ⚠ 不许用「万能键」（`…内容` / `…相关` / `…其他` / `…的事` / `…记录`）
- ⚠ 每天都会记的流水不许给键 —— 口诀：**这个键是给「会变的那一个值」用的吗？**

`remember` 的说明里也写了同样的两条。

⚠ 提示词是**劝**，不是**拦** —— 所以解析器里再机械挡一道：
键的最后一段正好是那些万能词的，一律当**留空**处理。
（只认「正好是」—— `本子.记录规则` 这种正经键不许被误杀。）

顺手补了一个漏洞：**带数字的键**（`她.身高170`）原来清洗那一路收得下、
提炼那一路挡得住 —— 两个解析器口径不一致。现在都挡。

### 自检

`清洗补键.mjs` 73 → 92 项、`事实键.mjs` 50 → 54 项：
万能键收不下、正经键不许误杀、老库里躺着的万能键不许拿它去收别人、
两份提示词口径必须一样。

## 1.1.3

### 修：每条下面那几行小字，看着像属于下一条

每条记忆下面会有一两行小字：「提炼前：…」「能搜到它的词：…」「讲的是：…」。

问题是那条**分隔线画在「行」上**，小字在线的**下面** ——
于是一眼看过去，它们像是**下一条**的，读起来全错位。

现在分隔线挪到**整条**上：行 + 下面那几行小字，一起框在一条里，
线画在**最后**。哪几行属于哪条，一眼就分得清。

（回收站里也一样 —— 「被取代的」那条下面的「为什么收起来」也在同一条里。）

### 自检

`排版.mjs` 17 → 20 项。新加的三条专门盯这个：

- 小字必须在「整条」**里面**（也就是在线的上面）
- 小字不许漂在「整条」**外面**（那就是老毛病又回来了）
- 行自己**不许**再画线（线画在行上，小字必然掉到线下面）

## 1.1.2

### 改：被取代的收进回收站

以前「已被取代」的条目还留在主列表里，只挂个牌子、划掉 —— 摆在那儿占地方，
还多一个牌子把正文挤窄。

现在它们收进「已删掉的」那一栏，跟回收站一起折叠。展开能看到一组
「被取代的（N）」，每条写着**为什么收起来**，点「恢复」就能让它回来。

⚠⚠ **它们不会被「清空」清掉。** 回收站里那排「全选 / 批量清掉 / 把已删掉的清空」
只管**你自己亲手删的**那些。被取代是自动标的（一次可能几百条），
不该被「清空」连坐销毁 —— 那是 2026-09-23 丢 157 条那次的教训。

界面上也写清了这一点，免得你以为「清空」把它们也清掉了。

### 顺手改的数

「会淡的 / 不会淡的 / 补日期」这些数原来把被取代的也算进去了 ——
它们已经收起来了，不该还占着名额。现在只数活着的。

### 自检

新增 `被取代归回收站.mjs`（29 项）。最要紧的一条是：
**「全选」只能数到亲手删的那几条，一条都不许把被取代的卷进去** ——
这条一旦退化，下一次「清空」就会重演那次事故。

## 1.1.1

### 改：这个功能的名字太术语了，换成白话

「事实键」「清洗补键」是造出来的词，看着不知道在说什么。全部换成说人话：

| 原来 | 现在 |
|---|---|
| 清洗补键 | 同一件事只留最新 |
| 把补的键全摘掉 | 撤掉这次标的 |
| 事实键：她.身高 | 讲的是：她.身高 |
| 给键 / 留空 | 标了 / 说不用标 |

按钮旁边还多一句解释：「『同一件事只留最新』是干嘛的：给老记忆一条条标上
**『这条讲的是哪件事』**。标好之后，讲同一件事的只留最新那条，旧的收起来（还能恢复）。」

### 修：撤回按钮上的数字对不上

「撤掉这次标的（N）」原来数的是**所有带标记的**，包括你自己手工填的；
可它真撤的只有**自动标的**那些 —— 于是按钮写 5、实际撤 2，看着像坏了。

现在按「真能撤几条」来数。手工填的那些本来就不归它管。

### 自检

`排版.mjs` 14 → 17 项：新增「界面上不许再出现『事实键』『清洗补键』这种自造词」——
这种词一不留神就会写回去，得盯着。

## 1.1.0

### 修：清洗补键跑一半就停、而且永远跑不完

老数据清洗里，模型会把一批判成「这条不用给键」（一次性的、拿不准的）。
那种**不写键**——可当时的代码把「没写出键」当成了**模型没答上来**：

- 连着 3 批就停下，报「模型没回出键」
- 更糟的是：判了留空的那批**没做记号**，下一趟又被挑出来 → 原地打转，永远洗不完

现在：

- 「回了但留空」算**问过了**（做记号，不再重复问）—— 那是一次判断，不是出错
- 只有模型**整批一声不吭**才算失败（不记号，下次重试）
- 撤回按钮把「问过」的记号一起清掉，撤完还能重洗
- 界面上多一句说明，讲清「判留空的那批去哪儿了」

### 修：记忆行的文字被挤成竖着的一条

行是横排的，但**不许折行**，右边又全是固定宽的东西（日期、鲜活度、重要度、原文、改、删…）。
键的牌子一加，两百多条记忆的行同时变挤，左边正文被压成**一个字一行**。

现在：

- 一行装不下就**自动折到下一行**
- 正文给个**最小宽度**，再也压不扁
- 键的牌子**搬出这一行**，挪到下面那行小字（跟「能搜到它的词」放一起）

### 自检

`清洗补键.mjs` 52 → 73 项：⚠判「留空」的必须算洗过、⚠整批没回才算出错、⚠撤回要清记号、
干跑一个字节都不写。

新增 `排版.mjs`（14 项）：⚠行必须能换行、⚠正文必须有最小宽度、键的牌子不许挂在行里 ——
这三条都会**悄悄退化**（随便加个牌子就犯病），所以必须有护栏。

## 1.0.9

### 新增：事实键（一条记忆讲的是「哪一件事」）

句子像不像，本来就不该拿来判「是不是同一件事」：

- 「身高一米七」和「长胖到 55 公斤」几乎是两件事，词面上却很像
- 「拼团还没开」和「拼团开了」是同一件事的前后，词面上却不像

现在每条可以带一个**事实键**，用「对象.方面」写（`她.身高`、`约定.身边位置`）。
**同一个键 = 同一件事** → 只留最新那条，旧的标成「已被取代」（不再注入，东西留着、能撤销）。

三个来源，都是顺手给的，不多花一次调用：

- `remember` 新增可选的「事实键」参数
- 提炼新增第六格「事实键」（模型改写时顺手出）
- 老数据清洗：设置页一个按钮，给还没有键的旧条目补上

⚠ 键里**不许写具体的值**（写 `她.身高`，不写 `她.身高170`）——
写了值就成了摘要，身高一变键就变，永远碰不上。拿不准就留空，留空完全没关系。

### 新增：一条只装一件事

「身高一米七、体重 52.5、大腿围 45、平时侧睡」原来是挤在一条里的。
挤成一条时，**只要里面有一件对不上，整条就配不上**，可它明明记着好几件对的事。

提炼提示词和 `remember` 的说明都写清了：一件事 = 一个属性、一个约定、一次事件，
同一批事实分成多行写。但同一个对象的一批同类事不用拆
（「讨厌油、讨厌烟味」照旧一条）。

### 老数据清洗（设置页）

- 先「试 8 条看看」：**一个字节都不写**，只把模型给的键摆出来给你看
- 再「直接开始洗」：一批 8 条，跑着就动，**只在设置页跑、不进对话**
- 洗完能「把补的键全摘掉」：回到洗之前 —— 只摘**自动补的**，自己手工填的键不动
- 动库之前自动另存一份

⚠ 键**只在模型或你明确给的时候**才算数。本地**绝不从正文里猜键** ——
猜错了会把一条正经记忆悄悄埋掉。

### 自检

`事实键.mjs`（50 项）、`清洗补键.mjs`（52 项）：键一样就只留最新、
键不一样的不许互相作废、没键的老条目不碰、分不出新旧不许乱埋、
干跑一个字节都不写、撤回不许动手工填的键。

## 1.0.8

### 修：组里混进一条短的，「真重复」那对就被拖着降级了

有两条明明几乎一模一样（像度 0.93、长度也差不多），却落在「汇总+明细」里 ——
不给一键合，得自己一组一组勾。

根因：**整组只看一个「长比」**。

```
那组 4 条：62字 / 42字 / 30字 / 29字
  30字 ↔ 29字   像度 0.91  长比 0.97   ← 明明是「真重复」
  可整组最长 62、最短 29 → 长比 0.47 → 整组降级成「汇总+明细」✗
```

组里只要混进一条很短的、或者一条「把好几件事缝在一起」的长条，
整个组就一起被降级 —— 明明像的那对也跟着不能一键合。

现在改成**组内按「对」再分一次**：

- 两两满足「真重复」（长比 ≥0.8 且像度 ≥0.85）的，**单独拎出来给一键合**
- 剩下的凑成「汇总+明细」（只剩 1 条的就不单独成组了）
- 输出顺序也调了：真重复的组排在前面

⚠ 这**只是把像的那对拎出来**，没有放松「真重复」的标准 ——
那一档仍然要长比 ≥0.8 且像度 ≥0.85。

### 自检

`查重.mjs` 27 → 30 项：短的混进来时，真重复那对必须被单独拎出来。

## 1.0.7

### 新增：过期条目标作废

有些记忆记的是**当时的状态**，会变：

- 「fumo 玩偶拼团还没开，不一定抢得到」
- 「B站投稿接口锁登录，我暂时读不到视频列表」
- 「她喜欢可爱的小裙子，但目前买不起」

它们**既不会被删、也不会淡掉** —— 淡忘只让**排序**往后掉，而门槛卡的是「词面证据」，
所以只要词对得上，一条过时的老记忆**照样会被注入**，看上去像现在的事实。

现在给记忆加了一个「**已被取代**」的标记：

- 标了之后**不再参与召回**；东西**留着**（设置页看得见、能撤销）
- ⚠ 它**只影响注入**，不动淡忘 —— 没被取代的照样走淡忘

**谁来标，两个来源：**

**① 本地自动** —— 新记的一条如果跟旧的**几乎一样**（词像度 ≥0.85 而且长短差不多），
旧的自动作废。这就是「同一个 key 只留最新一条」。
口径跟查重的「真重复」同一档（那一档 31 组逐组人眼核过，没有一组是误合）；
比这更松的不敢自动标 —— 标错了会**悄悄**让一条正经记忆不再被想起来。

**② AI 自己说** —— `remember` 多了一个可选的「取代」。
像「拼团开了」更新「拼团还没开」这种，**只有它自己知道**，本地算法判不出来；
它记的时候顺手填一下就行，零额外开销。

**界面上**：被取代的那条会**划掉**、挂一个「已被取代」的牌子，旁边有「恢复」。

### 修：库里只剩 1 条时，什么都想不起来（自检逮到的）

稀有度归一化的分母是 `log((N+1)/2)` —— `N=1` 时等于 **0**，
于是所有词的稀有度都算成 0 → 证据 C=0 → **一条都不给**。
新用户刚记下第一条、或者别的条目都被「跳过／作废」过滤掉时就会撞上，
表现是「记了东西却怎么都想不起来」，很难查。
现在库里 ≤1 条时不归一化：**有它就算数**。

### 自检

新增 `过期作废.mjs`（20 项）；一共 **31 份**。

## 1.0.6

### 新增：「已删掉的」那一栏补了**批量找回**

原来那一栏只有「批量清掉」——想全捞回来，只能**一条条点「找回来」**。
收着一百多条的时候，这根本干不了。

- **批量找回（N）**：勾了哪几条就捞哪几条；点了**先问一遍**再动手
  （这个不是销毁，是往回捞，所以文案跟「清掉」正好相反）
- **撤销合并：全找回（N）**：把标着「合并掉的」那批**一次全捞回来** ——
  等于**一键撤销整次合并**，不用一条条勾
- 捞回来的时候会把 `deletedAt`、「合并掉的」、「批量删的」这些记号一起清掉

### 自检

- `批删.mjs` 24 → 38 项：批量找回、撤销合并（只捞「合并掉的」、不许顺手把普通删的也捞回来）
- `批删界面.mjs` 40 → 52 项：按钮出得来、点了先问、确认才发、带对了参数

## 1.0.5

### 修：设置页上的版本号一直显示 v0.9.7

设置页写的那个版本号，是**写死**在代码里的一行常量 —— 发版时改了 `package.json`，
忘了改那一行。于是从 0.9.7 之后连着六版（0.9.8 / 0.9.9 / 1.0.0 ~ 1.0.4）都没跟上，
界面上一直显示 **v0.9.7**。

- 现在改成**从 `package.json` 读**
- 读不到就显示 `?` —— **不瞎猜一个版本号骗人**
- 自检里加了一条断言：`/list` 返回的版本号必须等于 `package.json` 的版本号

## 1.0.4

### 修：点一下把 123 条记忆合没了（真发生过）

「查重」面板里，**「按我选的合」原来会把每一组都合掉** —— 而每组默认都是选中的。
于是点一下，连「汇总+明细」那 105 组一起合了。
「汇总+明细」是**一长一短**（长的把短的包住了），长的那条未必包含短的每个细节，
**合了会丢东西** —— 这次丢了 123 条；回收站又被清空，常规备份也覆盖掉了。

两处都改了。

**一，「汇总+明细」默认不参与。**

- 每组多了一个「要合」的勾：**「真重复」默认勾上，「汇总+明细」默认不勾**
- 按钮从「按我选的合」改成「**按我勾的合（N 组、少 M 条）**」，只合勾上的
- 想合「汇总+明细」得自己勾那一组；另给一个「把「汇总+明细」也全勾上」的按钮
- 没勾的组上标着「（不参与）」，一眼看得出来

**二，真销毁之前另存一份。**

「清空」和「批量清掉」是**真销毁**。原来只靠两道常规备份，而它们都指望不上：

- `.bak` 只留**最后一次改动前**那一版（清空之后又被下一次写入覆盖了）
- 每天那份只留**当天最早**那一版

现在这两个动作之前，会另存一份带时间戳的到 `fish-memory\销毁前\`（留最近 20 份）。

⚠ 时间戳精确到**毫秒** —— 第一版只到秒，连着点几下会**同名互相覆盖**，
最后只剩 1 份（自检逮到的；真出事的时候正好就是连着点几下）。

### 自检

- 新增 `销毁前另存.mjs`（13 项）：清空 / 批量清掉之后，那份里被清掉的**还在**
- `查重界面.mjs` 20 → 29 项：默认勾选、只合勾上的、一键全勾
- 一共 **30 份**

## 1.0.3

### 提炼提示词加了两条规矩（用户反馈）

**一，正文里不许出现相对时间。**

有条记忆里写着「**前天** 12:00：09-17 她跟小编辑讨论设定」——
它是 09-17 那天记下的，过几天再看，「前天」就成了假的。

根因是改写的时候把**当时的口语时间词**照抄进了正文，而记忆是要存很久的。
现在要求：正文里不许出现「今天／昨天／前天／上周／刚才／一会儿」，
一律写成绝对日期（时间放第四格就行，正文里别用相对的词）。

**二，长短要匀。**

- 规则、喜好、习惯、约定这类，**一句能说清就别写两句**
  （「只喝矿泉水，不喝白开水」比「她喝水只喝矿泉水，从来不喝白开水」好）
- 过程、测试、来龙去脉**压成一句结论**，不要照搬原文的铺陈
- ⚠ 同时提醒了「**别为了短丢东西**」—— 事实、数字、名字、原话还是要留

⚠ 只对**以后新提炼的**生效。已经存着的老记忆要重跑一遍「提炼」才会按新规矩改写。

## 1.0.2

### 修：「已删掉的」里那两个确认条又跑到列表下面去了

点「把「已删掉的」清空」之后，那句「全清掉？找不回来了」渲染在**列表最下面** ——
收着几十条就得先翻到底才看得见问的是什么 ✗

这跟 0.9.7 修过的「工具条在最下面」是**同一个毛病**：
工具条挪上去了，但**点它之后出来的确认条没跟着挪**。

- 「批量清掉」的二次确认、「清空」的确认，现在**就地替换工具条**（都在最上面）
- 点了之后工具条那一格立刻变成确认条，**不用滚**
- 自检补了一条位置断言（确认条必须在第一条之前），以后退回去会被逮住

## 1.0.1

### 判重的尺子换了（原来那把一把都量不出来）

「查重」（找库里已经存着的重复）和「召回去重」原来都按**三字片段**算像度：
把句子切成所有相邻的 3 个字，再看两条共有多少个。

**这把尺子太脆**：两句话只要换几个字，整串三字片段就**全错位** ——
「她不是」「不是胃」「是胃寒」…… 全是跨词边界的碎片，一个都对不上。

实测在真库（782 条）里：

| 尺子 | 门槛 | 抓到的对数 |
| --- | --- | --- |
| 三字片段 | ≥0.85 | **0 对** |
| 按词比 | ≥0.85 | 85 对 |
| 按词比 | ≥0.75 | 135 对 |

同一件事的两条记忆，三字像度大多落在 0.33~0.83 —— 判重形同虚设。

### 换成「按词比」

- 像度 = 两条**共有的词数 ÷ 短的那条的总词数**
  （分母取短的 → 「长句包含短句」也算像）
- 用的是 1.0.0 装的那份通用中文词表，跟召回同一把刀
- **换说法不影响**：换掉几个词，其余的词照样对得上
- 一两个词的太短的不比 —— 「怕」「蓝色」这种单词撞上就算 1.0，那是假重复

### 门槛分两档

- **列出来给你挑**：像度 ≥ **0.75**
- **可以一键合**：像度 ≥ **0.85** 而且长度差不多（≥0.8）
  —— 自动合的那一档**守住不放宽**（合并会丢信息，虽然收在「已删」里能找回）

### 量出来的

真库 782 条：**136 组**（涉及 290 条），其中「真重复」31 组、「汇总+明细」105 组。
把 0.70~0.80 那一段逐条看过：绝大多数是真重复或「汇总+明细」；
31 组「可以一键合」逐组核过，**没有一组是误合**。

## 1.0.0

> 这一版动的是**切词**和**打分**两块底子，所以进了一位。

### 换了切词器：改用通用中文词表

原来判断「这句话跟这条记忆有没有关系」，用的是**字符串包含**：
把问句切出来的词，拿去记忆正文里搜有没有这个字串。

之所以只能用这种硬撞的办法，是因为老切法**会丢字**：它拿停用词当刀把句子切开，
切完的碎片对不上原文（「她的胃不好」被切成「胃不好」，「她的」凭空没了），
问句「她的胃」就只剩单字「胃」，跟记忆那边的「胃不」对不上。
于是只能退回字符串包含，把切词器的信息损失糊过去。

代价是**一个字会撞进长词里**：

- 问句里单独出现一个「车」→ 撞上记忆里的「**车**站」
- 「心」→ 撞上「关**心**」
- 「书」→ 撞上「**书**包」

这些字在库里都很稀有，于是被当成**强证据**，把不相干的记忆顶到最前面。

现在改用词表切：词表认得「车站」是一个词，那一段就**整段吃掉**，
「车」不再单独冒出来；而「那辆车」里的「车」是独立词，照样留着。
两边**终于能按词比**了。

- 词表用 jieba 的 `extra_dict/dict.txt.small`（MIT 协议，见 `lib/词典-来源.txt`），
  只保留词条本身，去掉了词频和词性两列
- 词表是**通用中文**的，跟谁用、说过什么话都无关 —— **装上第一天就管用**，
  不需要先喂够语料
- 最长匹配 + 单字兜底，**一个字都不丢**；兜底的单字如果是虚词（的／了／我）就不当线索
- 读不到词表文件时**自动退回老切法**，不会崩
- 有词表时**关掉「半个词」那道过滤**。那套（碎／整／邻熵）是给老切法擦屁股的 ——
  老切法会造出「天去」「去公」这种跨词碎片，才需要挑出来扔掉；
  词表切出来的是真词，再过滤只会**误杀真词**

### 打分：只拿「稀有词」算证据

治的是「**五个弱证据攒起来，看着像强证据**」。

原来命中的词是**按稀有度直接相加**的，于是「晚上＋会＋完＋吃＋面」这种
五个都不相关的词凑起来能把一条不相干的记忆顶到 0.73，
而真正相关的那条只撞上两个词、反而排不上去。

现在：**一个词如果在库里 1% 以上的记忆里都出现，它就不是线索，不拿它算分。**

- 「的」「了」「会」「完」自然被压掉；少见的具体词留下
- **不用维护虚词表** —— 中文虚词几百个，列不完，列了也是拍脑袋，
  而且列出来的表对别人不一定对
- 库里小的时候（刚装上）阈值自动变小 → 几乎不压 → 不会误伤
- 全被压光时**退回不压**（安全垫），免得一句话一个词都不剩

### 不硬凑条数（0.9.9 那批修补里漏掉的一处）

条数上限**跟着最高分走**：最高分很高（确实有一句很贴）→ 该给的都给；
最高分很低（一句贴的都没有）→ 只给 1 条，别硬凑。

- 位置很要紧：这个上限**在去重之后**才用，不然重复的会把名额吃掉
- 早先试过「相对门槛」（`C >= 0.7 × 最高分` 就留），**误杀**：
  分数低不等于不相关，只是那条撞得没那么准 → 放弃了

### 量出来的效果

拿 40 句真实问句、782 条真实记忆量。判据是「跟问句共享一个
**库里最多 3 条有的词**」（跟打分用的门槛不同，避免自己量自己）：

| | 前 3 名里捞到相关的 | 前 3 名精确度 |
| --- | --- | --- |
| 改之前 | 63% | 43% |
| 改之后 | 88% | 91% |

- 一轮召回 **2.3 ms**（比原来还快：按词比省掉了整段文本的字符串搜索）
- 词表 898 KB，**只在本地当查找表用，跟每轮对话发出去的 token 无关**

## 0.9.9

### 新功能：从会话生成档案

把会话读一遍，让模型提炼出一份长期档案（固定九章），再走已有的
「从旧档案导入」变成记忆条目。四步分开，因为**第三步要调模型、要花钱**，
得能一批一批停下来看：

- 记忆列表那一行加了个「从会话生成档案」按钮，点开是四步面板
- **① 选会话** —— 默认一个都不勾（不替用户决定要处理哪些）；
  有「全选 / 全不选」
- **② 算一下要多少** —— 这一步**不调模型、不花钱**，只报批数、字数、粗算 token
- **③ 开始跑** —— 一批一批调模型；跑完一批就把条目攒起来，
  **下一批会带上「已经记过的」清单**，同一件事不会被收两遍
- **④ 存到哪儿** —— 能直接改，也能点「选文件夹」挑；目录不存在会自动建；
  给的是目录（结尾带分隔符、或本身是已存在的目录）会自动补上文件名；存之前备份

会话列表这块：

- **显示会话名字**（原来只有 `session-xxxx`，没法挑）
- 按 **DSH 自己的名单**列（不再去翻磁盘目录 —— 磁盘上会有 DSH 早就不显示的残留）
- 过滤掉：**归档的**、**子代理跑出来的**、**一句话都没说过的空壳**
- 子代理靠会话头里的 `delegationDepth > 0` 认出来（只解第一帧，便宜）

### 召回质量

- **稀有度夹到 1 以内。** 原来没夹：一个在库里**从来不是一个词**的字符串
  （`df = 0`）会算到 **1 以上** —— 实测某条拿到 1.12，比任何真词都高。
  而 `df = 0` 多半意味着那是假词或单字，**越假反而越值钱** ✗
- **打分乘上「说话稀有度」。** 这张尺子（从用户自己的话里长出来，
  天天挂在嘴边的词不算信息）原来只用在**问句门槛**上，没用在**打分**上 ——
  于是「宝宝」这种天天喊的词照样当满额证据。现在它也参与打分。
  样本少于 20 句时不压，免得新会话被误伤。
- **召回去重门槛另开一个常量（0.80），不再跟「查重合并」共用 0.85。**
  合并是**真删东西、不可逆**，必须保守；召回时去重只是**这一轮不显示它**，
  东西还在库里、下一轮照样能想起来 —— 风险小得多，就该用更宽的门槛。

### 修

- 「选文件夹」选过一次之后再想换会卡住：再打开时**不重新读目录**，
  看到的还是上次那份。现在每次打开都重读，并且**从当前路径所在的目录开始**
  （不用每次从盘符点起）；选择器里也放了一个「收起」
- 自检套件加了 180 秒超时。有的自检结尾是 `if (挂 > 0) process.exit(1)` ——
  **挂 = 0 时不退出**，而它里面有挂着的定时器 → 进程永远不结束 →
  整套自检看起来像卡死，最后那行总结永远打不出来 ✗

## 0.9.8

- 「补标不淡 / 重判一遍」的进度条**挪到它自己的按钮下面**。
  原来它跟「扩词」「提炼」挤在同一张卡片里 —— 点了重判要往上翻才看得见，
  没点的人还会以为那条是自己点出来的 ✗
  现在谁点的进度条就跟在谁的按钮旁边，**点了才出现，没点就没有**；
  条上写明是「补标不淡」还是「重判一遍」。
- 「补标不淡」的判据补了一条例外：**日期只是一票，不是否决票**。
  原来「以日期开头一律判成会淡」是一票否决，于是「某天定下、往后一直算数」
  那类被整条判淡了 —— 比如纪念日、定下来的口令、说好一辈子的事。
  现在这类**以日期开头也判不淡**，判据看的是：把日期拿掉之后，
  剩下的话是不是一条往后一直成立的说法，而不是一段「那天怎么了」的经过。
  纯流水账仍然判会淡，没有放松。

## 0.9.7

- 「已删掉的」那一栏的工具条**挪到最上面**。
  原来它在列表最下面 —— 几十条的时候得先翻到底才能点「全选」。
  现在紧挨着标题：全选 / 批量清掉 / 取消勾选 / 全部清空。

## 0.9.6

- 修「补标不淡」**跑到一半自己停掉**：判断「跑完没」原来用的是「这批判回了几条」——
  某一批模型没回东西（判回 0 条）就被当成「判完了」，直接停下 ✗
  （实际记录里是「判了 0 条，还剩 665」，然后就没了）
  - 改成看服务端返回的 `done`；一批回 0 条就**接着试下一批**
  - 连着 3 批都空才停，并说清楚「再点一次会接着跑，不会重头来」

## 0.9.5

- **「补标不淡」配了进度条**。它原来只有一行字，跑起来看不出在动 ——
  其实一直在跑（一批批都有记录），但界面上像卡住了。
  现在有一条进度条，写着「补标不淡：已判 N / 总共 M 条（X%）　还剩 K 条」，
  撞限流等待时会写明「正在等 N 秒」。

## 0.9.4

- 「已删掉的」那一栏补了**全选 / 取消全选**：
  点「全选（N）」一下全勾上（按钮变成「取消全选」），再点一下取消。
  勾完照样走「批量清掉」的两次确认。

## 0.9.3

- **「已删掉的」那一栏也能批量清掉了**。原来只有「一条条清」和「全部清空」，
  想清掉其中几条得一条条点。现在：
  - 每行一个勾选框 → 勾上几条就出现「**批量清掉（N）**」
  - **点第一次不清** —— 只出确认条；在确认条上**再点一次**才真清（这个是销毁，所以要问两遍）
  - 跟「批量删」用的是各自独立的勾选，两栏不会互相带

## 0.9.2

- 判据补一条：**没有日期的，按内容本身判**，别按长短 ——
  讲性格、关系、称呼、喜好、身体和病史、长期状况 → 1；
  讲某一次对话、某一个瞬间、某一时的心情、某一次的数据 → 0.5。
  0.9.1 只写了「以日期开头的默认 0.5」，没有日期的那些缺一条判据，
  结果把一批讲性格、讲关系定义的也判成了会淡的。

## 0.9.1

- **「补标不淡」的判据写严了**。原来判得太松：把一批「某天发生了什么」的
  流水账也判成了「要一直记得」（实测判过的里面 82% 都判成了长期事实）。
  新判据：
  - **以日期开头的，默认一律 0.5** —— 哪怕那件事很重要、很感人、是第一次、是求婚、是婚礼。
    「重要」不等于「一直成立」：重要的事值得记住，但它记的是「那天」。
  - **唯一的例外**：整条就是「约定 / 规则 / 承诺的条文」本身。而且要同时满足三条：
    删掉日期后仍是一条完整可照做的规定；是「往后一直照做」的；不夹杂当天的时间、
    金额、动作、心情、地点等流水细节。缺一条就回 0.5。
  - 兜底收紧：日期开头的条目，**只有能原样引用出一句条文**时才给 1。
- 修：列表接口没把「判过没」发给页面 —— 于是「补标不淡」的计数永远是全库条数，
  看着像要从头重来（服务端本来就只判没判过的）。
- 新增**「重判一遍」**：判据改过之后，把老结果清掉用新判据重来一遍。
- 「补标不淡」撞上限流（429）时**自动等一等再试**（5s→10s→20s→40s→60s，最多 6 次），
  不再直接失败；每批之间的间隔也从 0.3 秒放慢到 1.5 秒。

## 0.9.0

- **「不会淡」的判据改回「长期事实」**（`category === 'stable_profile'`）。
  0.8.9 曾用「鲜活度是否恒定」当判据，那是打分实现细节，不是语义，
  会把一批没有日期的普通事件误收进「不会淡」里。
- **没有日期的记忆现在也会淡**：以「记录时间」（`createdAt`）为衰减锚点。
  此前这类记忆的鲜活度被钉在常数上，看起来永远不淡。

## 0.8.9

- **「不会淡的」单独收进一个折叠分组**，外面只列会淡的。
  长期事实是要一直记得的事，不该和普通事件混在一列里。
- 长期事实的鲜活度一栏显示「不淡」，不再显示百分比。

## 0.8.8

- 新增**批量删除**（两次确认）：
  每行一个勾选框 → 出现「批量删（N）」→ 点一下只出确认条 → 再点「确定删」才真删。
  删掉的收进「已删除」，可以找回；操作前自动备份。

## 0.8.7

- 修「鲜活度」显示：
  - 列表页原本自己算了一套公式，和召回实际使用的不是同一套；
    没有日期的记忆会算出 `NaN`，页面显示成「鲜活 NaN%」。
    现在两边共用同一个函数。
  - 长期事实不再显示「100%」（它本来就不淡，显示百分比会误导）。
- 日期显示两处：
  - **未来的日期被显示成「今天」**（`days <= 0` 把负数也算进去了）。
  - 47 天被四舍五入说成「2 个月前」，改成向下取整。

## 0.8.6

- **正文里提到的「以后」的日期不算事件日期**。
  例如一条记录规则的消息里写了「下次 X 月 X 日」，
  原来会把那个未来日期当成这条记忆的发生时间。

## 0.8.5

- **重写日期提取**：
  - 开头的日期才是事件日期，认到就不再往后找。
  - 句中提到的生日 / 出生 / 纪念日一律不算。
  - 只有生日、没有事件日期时留空。
  - 「vs / 还是 / 不是」这种比较或纠正数字的语境不算日期。
  - 年份改为「取离现在最近的那一年」（原来一律按过去算，
    会把明天的日期算成去年）。

## 0.8.4

- 修「查重」两个问题：
  1. 归组原用并查集（传递闭包），A 像 B、B 像 C 就归为一组，
     但 A 和 C 可能并不相似，一组会串成杂烩。
     改成按种子归组：每条只和种子比较。
  2. 合并接口原本不校验，客户端传什么就删什么。
     改成服务端自己再算一遍相似度，不够像的一条都不收，并列出来。
- 合并请求改为传「组」（保留哪条 + 删除哪些）。

## 0.8.3

- 新增**查重**入口（列表标题行）。去重此前只在召回和导入时自动进行，
  库里已有的重复没有入口。
  - 分两类：**真重复**（两条长度接近，可一键合并）和
    **汇总 + 明细**（一长一短，只列出让你自己挑）。
  - 默认保留最长的一条；每组可单独选择保留哪条。
  - 合并是软删除，收进「已删除」，可以找回。
- 修：会话详情页看不到任何结果提示。
  那些提示原来只在第一级渲染，而详情页是提前返回的。

## 0.8.2

- **涉及第三方时必须写名字**（助手、别的 AI、孩子、家人、朋友）。
  提炼此前只有「我 / 她」两个人称，第三方会被并进去，
  导致「某人做的事」被记成「我做的」或「她做的」。

## 0.8.1

- 提炼的人称规则补三条：
  - 原文已有主语的，一个字都不许换。
  - 宁可分成几句写，也不要为了通顺把几个分句揉成一句（一揉主语就串）。
  - 只有说得出依据才补主语，说不出依据就留空。

## 0.8.0

- **「补标不淡」改成能上也能下**。
  原来只找「该标记却没标记」的，治不了另一半：
  「不该标记却标记了」。现在所有没判过的一起判，
  模型说 1 就升成长期事实，说 0.5 就降回普通事件。

## 0.7.9

- 设置页整理：「参数」默认收起；「备份」说明移到末尾。
- 图标 🐟 → 🐋。

## 0.7.8

- **导入时也做近似去重**：相似度足够高时，新的更全就补全旧的，否则跳过。
  阈值比召回去重（0.85）高，取 0.9 —— 导入不可逆，宁可漏几条重复。

## 0.7.7

- 「长期事实」单独收成一栏（折叠分组）。
- 新增**补标不淡**按钮：拿模型一条条判「该不该一直记得」。

## 0.7.6

- 提炼接上「永远不淡」：顺手判这一条该不该一直记得。
- 「重要的日子」（生日、纪念日、结婚、第一次）补进判据清单。
- 寒暄、撒娇、甜话的汇总在提炼时判「跳过」。

## 0.7.5

- **召回去重**：注入前把几乎一样的只留一条（留分高的）。

## 0.7.4

- `package.json` 补 `keywords` / `repository`，整理 README 与格式。

## 0.7.3

- 修开关：关掉之后注入会立刻停。

## 0.7.2

- 修安装说明：包没有发布到 npm，改用 GitHub 形式安装。
- 提炼不再写死用户称呼（跟随设置）；出生日期不当成事件时间；
  短记忆门槛从 8 字降到 6 字。

## 0.7.1

- 导入健壮性一批（13 处）：一整段没分行、单行超长、子文件夹、
  各种项目符号与括号编号、零宽空格、时区不写死、不存在的日期等。

## 0.7.0

- 发布到 GitHub（MIT）。此前只在本机使用。

## 0.6.x

- 0.6.6 抠日期强化 + 「补日期」按钮。
- 0.6.5 跨年显示必须带年份。
- 0.6.4 没有时间戳、但被再次提到的记忆要能涨、也要会淡。
- 0.6.3 **门槛从卡 P 改成卡 C（证据）**，鲜活度只参与排序。
- 0.6.2 正文不撑破容器（`minWidth: 0` + `overflowWrap`）。
- 0.6.1 记忆列表按日期排序。
- 0.6.0 幽灵会话不再显示；归档筛选不再静默失败；版式整理。

## 0.5.x 及更早

- 记忆段从 `systemPrompt.section()` 改成 `context()`：
  每轮都变的内容放进系统提示词会让整段缓存失效。
- trace 瘦身 + 切词缓存（每轮开销从约 57 ms 降到约 8 ms）。
- 提炼（档案原文 → 第一人称记忆）、扩词、导入、召回面板、
  记忆列表检索与折叠、复制记忆到别的会话、归档会话不再显示。
